name: branch-test

on:
  workflow_dispatch:
jobs:
  echo-test:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || contains(github.ref, 'test')
    steps:
      - name: echo-test-do-something
        run: |
          echo "Success"

